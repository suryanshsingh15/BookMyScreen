# Only needed if backend runs on a different host/port than the Vite proxy assumes.
# Leave blank to use the Vite dev proxy (see vite.config.js) during local development.
VITE_API_URL=
VITE_SOCKET_URL=
